<%= form_for(@whatever) do |f| %>
  <% if @whatever.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@whatever.errors.count, "error") %> prohibited this whatever from being saved:</h2>

      <ul>
      <% @whatever.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :available %><br>
    <%= f.check_box :available %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
